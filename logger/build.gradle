plugins{
	id "java-library"

	id "jutils"
	id "jutils.jar.libs"
}

description = property("logger.description")

dependencies{
	api project(":base")
}

publishing{
	publications{
		maven(MavenPublication){
			from components.java

			pom{
				name = "${projectPrettyName} - Logger"
				description = project.description
			}
		}
	}
}